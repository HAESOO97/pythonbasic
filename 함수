##함수의 구조
#def 함수명(매개변수):
  #실행할 소스코드
  #return 반환 값
def add(a,b):
  return a+b
 print(add(3,7))
 
 ##return문 없이 작성하는 방법
 def add(a,b):
  print('함수의 결과 : ' ,a+b)
 add(b=3,a=7)
 
 #함수 안에서 함수 밖의 데이터를 변경하는 경우
 a=0
 def func():
  global a
  a+=1
 for i in range(10):
  func()
 print(a)
 
 #람다 표현식
 #일반적인 메서드 사용방식
 def add(a,b)
  return a+b
 print(add(3,7))
 #람다 표현식으로 구현한 add()메서드
 print((lambda a,b:a+b)(3,7))
 
